// Basic widget test placeholder.
// Firebase and ML Kit require device/emulator — full testing done manually.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    expect(1 + 1, 2);
  });
}
